# react-typescript-web-vite-template
template for react web projects using typescript and vite
set up with Redux and thunk, an example auth (user) reducer and actions, and redux hooks (selector and dispatch)

# First things first:
## yarn install
